name: my_journal_lite
description: A new Flutter project generated by Hyver Studio.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.7.12 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_lints: ^3.0.0
  firebase_core: ^2.30.1
  firebase_database: ^10.5.4
  supabase_flutter: ^2.5.0
  flutter_dotenv: ^5.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
